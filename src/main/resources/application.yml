server:
  port: ${PORT:8080}

spring:
  application:
    name: points-mall-core
  # Disable Spring Boot Actuator auto-configuration; health is handled by custom endpoint
  autoconfigure:
    exclude:
      - org.springframework.boot.actuate.autoconfigure.web.server.ManagementContextAutoConfiguration
